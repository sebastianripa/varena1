#include <fstream>

using namespace std;

ifstream fin ("resturi.in");
ofstream fout ("resturi.out");

int main()
{
    long long a, b, c, i, s = 0;
    fin >> a >> b >> c;
    for(i = a; i <= b; i++)
    {
\    }
    fout << s;
    return 0;
}
