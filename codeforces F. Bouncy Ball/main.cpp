#include <iostream>

using namespace std;

int main()
{
    int t, n, m, i1, j1, i2, j2, i, j, b = 0;
    char d1, d2;
    cin >> t;
    while(t--)
    {
        cin >> n >> m >> i1 >> j1 >> i2 >> j2;
        cin.get();
        cin.get(d1);
        cin.get(d2);
        while(i1 != i2 && j1 != j2 && c != 2 && n != m)
        {

        }
    }
    return 0;
}
