#include <iostream>

using namespace std;

int main()
{
    int a,b, ok, cop, k = 0;
    cin >> a >> b;
    cop = a;
    while(ok == 0)
    {
        if(a%b == 0)
        {
            k += a/b-1
            ok = 1;
        }
        else
        {
            k += x/y+2;
            a =
        }
    }
    return 0;
}
