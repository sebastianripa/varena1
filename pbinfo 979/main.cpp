#include <iostream>

using namespace std;

int main()
{
    int n, k, x, cx, maxi = 0, i, j, nr, cm = 0;
    cin >> n >> k;
    for(i = 1; i <= n; i++)
    {
        cin >> x;
        if(x%2 == 0)
            maxi = max(x, maxi);
        cn = n;
        nr = n;

    }
    return 0;
}
